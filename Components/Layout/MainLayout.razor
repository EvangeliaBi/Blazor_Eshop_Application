@inherits LayoutComponentBase       // Το LayoutComponentBase επιτρέπει τη χρήση του @Body, όπου θα αποδίδεται το περιεχόμενο των σελίδων.
@using MyBlazorApp.Services         @*Κάνει διαθέσιμο το namespace MyBlazorApp.Services. Απαιτείται για να χρησιμοποιηθεί το CartService.*@
@inject CartService MyCart          // Εισάγεται μέσω του (Dependency Injection) το CartService στο layout. Το αντικείμενο MyCart περιέχει τα προϊόντα του καλαθιού και είναι κοινό σε όλες τις σελίδες.


<div class="page d-flex">

    <!-- Sidebar -->
    <div class="sidebar bg-light p-3">
        <NavMenu />
    </div>

    <!-- Main content -->
    <div class="flex-fill d-flex flex-column">

        <!-- Top navbar -->
        <div class="top-row px-4 py-2 d-flex justify-content-between align-items-center border-bottom bg-white shadow-sm">
            <a href="/" class="text-decoration-none fw-bold fs-4">Tech E-Shop</a>

            <div class="d-flex align-items-center">
                

                <NavLink class="btn btn-outline-secondary" href="/">    <!--Μόλις πατήσει ο χρήστης το Logout ανακατευθύνεται στην αρχική σελίδα του e-shop.-->
                    <i class="bi bi-box-arrow-right"></i> Logout
                </NavLink>
            </div>
        </div>

        <!-- Page body -->
        <main class="flex-fill p-4">
            @Body
        </main>
    </div>
</div>

<div id="blazor-error-ui" data-nosnippet>
    An unhandled error has occurred.
    <a href="." class="reload">Reload</a>
    <span class="dismiss">🗙</span>
</div>
